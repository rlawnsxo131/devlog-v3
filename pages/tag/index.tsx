import { css } from '@/styles/_stitches.config';

interface Props {}

function TagPage(props: Props) {
  return <div className={block()}>tag page</div>;
}

const block = css({});

export default TagPage;
